# Python dependencies for reference data extraction
# Install with: pip install -r requirements.txt

torch>=2.0.0
transformers>=4.40.0
numpy>=1.24.0

# Optional: for faster inference
# accelerate>=0.20.0
